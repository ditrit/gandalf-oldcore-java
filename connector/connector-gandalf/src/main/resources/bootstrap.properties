# CORE
spring.application.name=connector
server.error.whitelabel.enabled=false

# CLUSTER
instance.name=cluster_test

# AGGREGATOR
aggregator.name=aggregator_test
aggregator.service.endpoint=tcp://127.0.0.1:9021

# CONNECTOR
connector.name=connector_workflow-engine_test
connector.type=workflow-engine
connector.target.type=zeebe
connector.target.endpoint=127.0.0.1