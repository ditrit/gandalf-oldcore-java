# CORE
spring.application.name=test-command
spring.cloud.config.uri=http://localhost:8888

# CLUSTER
instance.name=cluster_test

# CONNECTOR
connector.type=client
connector.name=connector_command_test

# TOOL
spring.profiles.active=h2
