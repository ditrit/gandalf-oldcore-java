# CORE
spring.application.name=worker
server.error.whitelabel.enabled=false

# CLUSTER
instance.name=cluster_test

# CONNECTOR
connector.name=connector_workflow-engine_test

# WORKER
worker.name=worker_test
worker.type=workflow-engine
worker.target.endpoint=127.0.0.1
worker.service.endpoint=tcp://127.0.0.1:9021
worker.command.connection=ipc://worker/command/
worker.event.connection=ipc://worker/event/
